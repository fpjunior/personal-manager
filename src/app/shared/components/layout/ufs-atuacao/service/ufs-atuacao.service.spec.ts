/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UfsAtuacaoService } from './ufs-atuacao.service';

describe('Service: UfsAtuacao', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UfsAtuacaoService]
    });
  });

  it('should ...', inject([UfsAtuacaoService], (service: UfsAtuacaoService) => {
    expect(service).toBeTruthy();
  }));
});
